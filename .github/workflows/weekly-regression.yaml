name: Weekly Regression

on:
  schedule:
    - cron: '0 6 * * 6'  # Saturday 6 AM UTC

  workflow_dispatch:
    inputs:
      framework:
        description: 'Framework to run'
        required: false
        type: choice
        options:
          - all
          - onnx
          - torch
        default: 'all'

      baseline_source:
        description: 'Branch to compare baseline against'
        required: false
        type: choice
        options:
          - own
          - develop
          - previous-release
          - custom
        default: 'own'

      baseline_source_custom:
        description: 'Custom branch name (only used when baseline_source is "custom")'
        required: false
        type: string
        default: ''

env:
  VER_PYTHON: "3.10"

jobs:
  #=============================================================================
  # Build AIMET wheels (parallel builds)
  #=============================================================================
  build-aimet-onnx-gpu:
    name: Build AIMET (ONNX + GPU)
    if: ${{ github.event.inputs.framework != 'torch' }}
    uses: ./.github/workflows/build-wheels.yml
    with:
      # TODO: runs-on can be removed once ephemeral runners are fully adopted —
      # the runner-id is dynamically generated and does not use this value.
      variants: >
        {"include":[{
          "id":"onnx-gpu",
          "runs-on":"k8s-gpu",
          "VER_PYTHON":"3.10",
          "VER_ONNXRUNTIME":"1.19.2",
          "VER_CUDA":"12.6.3",
          "ENABLE_TESTS":"ON",
          "PIP_INDEX":""
        }]}
      image-tag: latest
      use-ephemeral-runner: true
    secrets: inherit

  build-aimet-torch-gpu:
    name: Build AIMET (Torch + GPU)
    if: ${{ github.event.inputs.framework != 'onnx' }}
    uses: ./.github/workflows/build-wheels.yml
    with:
      # TODO: runs-on can be removed once ephemeral runners are fully adopted —
      # the runner-id is dynamically generated and does not use this value.
      variants: >
        {"include":[{
          "id":"torch-gpu",
          "runs-on":"k8s-gpu",
          "VER_PYTHON":"3.10",
          "VER_TORCH":"2.*",
          "VER_CUDA":"12.6.3",
          "ENABLE_TESTS":"OFF",
          "PIP_INDEX":""
        }]}
      image-tag: latest
      use-ephemeral-runner: true
    secrets: inherit

  #=============================================================================
  # ONNX Weekly Regression (GPU Runner 1)
  #=============================================================================
  start-runner-onnx:
    name: Start runner (onnx-gpu weekly)
    needs: build-aimet-onnx-gpu
    if: ${{ github.event.inputs.framework != 'torch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/start-gpu-runner
        with:
          runner-id: onnx-gpu-weekly-${{ github.run_id }}-${{ github.run_attempt }}
          gpu-request: "1"
          cpu-request: "16"
          memory-request: "160Gi"
          docker-image: ${{ vars.DOCKER_REGISTRY }}/${{ vars.DOCKER_IMAGE }}-onnx-gpu:latest
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  onnx-weekly:
    name: Run ONNX Weekly (GPU)
    if: ${{ github.event.inputs.framework != 'torch' }}
    needs: start-runner-onnx
    runs-on: onnx-gpu-weekly-${{ github.run_id }}-${{ github.run_attempt }}

    timeout-minutes: 720

    env:
      CUDA_VISIBLE_DEVICES: "0"
      PYTHONUNBUFFERED: "1"
      UV_LINK_MODE: "symlink"
      BASELINE_SOURCE: ${{ github.event.inputs.baseline_source == 'custom' && github.event.inputs.baseline_source_custom || github.event.inputs.baseline_source || 'own' }}
      VER_TORCH: "2.*"
      VER_CUDA_SHORT: "cu126"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache models and datasets
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache
            ~/.qai_hub_cache
            ~/.qaihm
            ~/.cache/huggingface
            ~/.cache/torch
          key: aimet-weekly-onnx-cache-${{ runner.os }}-${{ hashFiles('AIMETRegression/**') }}
          restore-keys: aimet-weekly-onnx-cache-${{ runner.os }}-

      - name: Download AIMET wheel
        uses: actions/download-artifact@v3
        with:
          name: onnx-gpu-wheel
          path: downloads

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 \
            libxrender-dev libgomp1 git openssh-client unzip curl
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*

      - name: Setup Python environment with UV
        run: |
          set -e
          sudo apt-get update -qq
          sudo apt-get install -y python${{ env.VER_PYTHON }} python${{ env.VER_PYTHON }}-venv python${{ env.VER_PYTHON }}-dev

          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"

          uv venv .venv --python python${{ env.VER_PYTHON }}
          . .venv/bin/activate

          # Pin torch before qai-hub-models; otherwise qai-hub-models pulls a torch version incompatible with the pod's CUDA driver.
          # --extra-index-url is required on every subsequent install so uv cannot re-resolve torch onto a CUDA-13 wheel from plain PyPI.
          uv pip install torch==${{ env.VER_TORCH }} torchvision --index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install qai-hub qai-hub-models --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install onnxruntime-gpu==1.19.2 --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install pynvml pytest requests --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}

      - name: Install AIMET
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          uv pip install downloads/*.whl --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          python -c "import aimet_onnx; print('AIMET version:', aimet_onnx.__version__)"

      - name: Verify ONNX Runtime GPU
        run: |
          . .venv/bin/activate
          python -c "
          import onnxruntime as ort
          providers = ort.get_available_providers()
          print('ONNX Runtime providers:', providers)
          assert 'CUDAExecutionProvider' in providers
          "

      - name: Configure AI Hub
        env:
          QAI_HUB_API_TOKEN: ${{ secrets.AIMET_BOT_AIHUB_DEV_TOKEN }}
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.utils configure-aihub

      - name: Download baseline from previous runs
        id: download_baseline
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.artifacts download-baseline \
            --output-dir AIMETRegression/baselines/downloaded

      - name: Setup baseline for comparison
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.utils setup-baseline \
            --suite "weekly-onnx"

      - name: Run test suite
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.suite_runner --suite weekly-onnx

      - name: Compare with baseline and generate report
        run: |
          . .venv/bin/activate

          SUITE="weekly-onnx"
          RESULTS_CSV="AIMETRegression/reports/results_${SUITE}.csv"

          if [ ! -f "$RESULTS_CSV" ]; then
            echo "Results CSV not found: $RESULTS_CSV"
            exit 1
          fi

          python AIMETRegression/baseline_comparison.py run \
            --results "$RESULTS_CSV" \
            --suite-name "$SUITE" \
            --baselines-dir "AIMETRegression/baselines" \
            --baseline-source "${{ steps.download_baseline.outputs.source_branch || env.BASELINE_SOURCE }}" \
            --github-summary \
            --output-file "AIMETRegression/reports/comparison_summary.json"

      - name: Generate environment lockfile
        if: always()
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          python -m AIMETRegression.workflow.utils generate-lockfile \
            --run-id "${{ github.run_id }}"

      - name: Generate timestamp for artifacts
        if: always()
        id: timestamp
        run: |
          echo "datetime=$(date '+%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Generate baseline artifact name
        id: artifact_name
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          ARTIFACT_NAME=$(python -c "
          from AIMETRegression.workflow.artifacts import ArtifactManager, WorkflowConfig
          config = WorkflowConfig.from_env()
          manager = ArtifactManager(config)
          print(manager.get_artifact_name('${{ github.ref_name }}', 'weekly-onnx'))
          ")
          echo "name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT
          echo "Generated artifact name: $ARTIFACT_NAME"

      - name: Upload baseline artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ steps.artifact_name.outputs.name }}
          path: AIMETRegression/baselines/latest.json
          retention-days: 30
          if-no-files-found: error

      - name: Upload comparison summary
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: onnx-weekly-comparison-summary
          path: AIMETRegression/reports/comparison_summary.json
          retention-days: 10
          if-no-files-found: warn

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: onnx-weekly-reports-${{ steps.timestamp.outputs.datetime }}-${{ github.run_id }}
          path: AIMETRegression/reports/**/*
          retention-days: 30

  cleanup-runner-onnx:
    name: Cleanup runner (onnx-gpu weekly)
    if: always() && github.event.inputs.framework != 'torch'
    needs: [ start-runner-onnx, onnx-weekly ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/stop-gpu-runner
        with:
          runner-ids: onnx-gpu-weekly-${{ github.run_id }}-${{ github.run_attempt }}
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  #=============================================================================
  # Torch Weekly Regression (GPU Runner 2, runs in PARALLEL)
  #=============================================================================
  start-runner-torch:
    name: Start runner (torch-gpu weekly)
    needs: build-aimet-torch-gpu
    if: ${{ github.event.inputs.framework != 'onnx' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/start-gpu-runner
        with:
          runner-id: torch-gpu-weekly-${{ github.run_id }}-${{ github.run_attempt }}
          gpu-request: "1"
          cpu-request: "16"
          memory-request: "160Gi"
          docker-image: ${{ vars.DOCKER_REGISTRY }}/${{ vars.DOCKER_IMAGE }}-torch-gpu:latest
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  torch-weekly:
    name: Run Torch Weekly (GPU)
    if: ${{ github.event.inputs.framework != 'onnx' }}
    needs: start-runner-torch
    runs-on: torch-gpu-weekly-${{ github.run_id }}-${{ github.run_attempt }}

    timeout-minutes: 720

    env:
      PYTHONUNBUFFERED: "1"
      PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"
      UV_LINK_MODE: "symlink"
      BASELINE_SOURCE: ${{ github.event.inputs.baseline_source == 'custom' && github.event.inputs.baseline_source_custom || github.event.inputs.baseline_source || 'own' }}
      VER_TORCH: "2.*"
      VER_CUDA_SHORT: "cu126"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache models and datasets
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache
            ~/.qai_hub_cache
            ~/.qaihm
            ~/.cache/huggingface
            ~/.cache/torch
          key: aimet-weekly-torch-cache-${{ runner.os }}-${{ hashFiles('AIMETRegression/**') }}
          restore-keys: aimet-weekly-torch-cache-${{ runner.os }}-

      - name: Download AIMET wheel
        uses: actions/download-artifact@v3
        with:
          name: torch-gpu-wheel
          path: downloads

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 \
            libxrender-dev libgomp1 git openssh-client unzip curl
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*

      - name: Setup Python environment with UV
        run: |
          set -e
          sudo apt-get update -qq
          sudo apt-get install -y python${{ env.VER_PYTHON }} python${{ env.VER_PYTHON }}-venv python${{ env.VER_PYTHON }}-dev

          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"

          uv venv .venv --python python${{ env.VER_PYTHON }}
          . .venv/bin/activate

          # Pin torch before qai-hub-models; otherwise qai-hub-models pulls a torch version incompatible with the pod's CUDA driver.
          # --extra-index-url is required on every subsequent install so uv cannot re-resolve torch onto a CUDA-13 wheel from plain PyPI.
          uv pip install torch==${{ env.VER_TORCH }} torchvision --index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install qai-hub qai-hub-models --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install pynvml pytest requests object-detection-metrics --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}

      - name: Install AIMET
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          uv pip install downloads/*.whl --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          python -c "import aimet_torch; print('AIMET Torch version:', aimet_torch.__version__)"

      - name: Verify PyTorch GPU
        run: |
          . .venv/bin/activate
          python -c "
          import torch
          print('PyTorch version:', torch.__version__)
          print('CUDA available:', torch.cuda.is_available())
          if torch.cuda.is_available():
              print('CUDA device:', torch.cuda.get_device_name(0))
              print('CUDA version:', torch.version.cuda)
          assert torch.cuda.is_available(), 'CUDA not available!'
          "

      - name: Configure AI Hub
        env:
          QAI_HUB_API_TOKEN: ${{ secrets.AIMET_BOT_AIHUB_DEV_TOKEN }}
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.utils configure-aihub

      - name: Download baseline from previous runs
        id: download_baseline
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.artifacts download-baseline \
            --output-dir AIMETRegression/baselines/downloaded

      - name: Setup baseline for comparison
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.utils setup-baseline \
            --suite "weekly-torch"

      - name: Run test suite
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.suite_runner --suite weekly-torch

      - name: Compare with baseline and generate report
        run: |
          . .venv/bin/activate

          SUITE="weekly-torch"
          RESULTS_CSV="AIMETRegression/reports/results_${SUITE}.csv"

          if [ ! -f "$RESULTS_CSV" ]; then
            echo "Results CSV not found: $RESULTS_CSV"
            exit 1
          fi

          python AIMETRegression/baseline_comparison.py run \
            --results "$RESULTS_CSV" \
            --suite-name "$SUITE" \
            --baselines-dir "AIMETRegression/baselines" \
            --baseline-source "${{ steps.download_baseline.outputs.source_branch || env.BASELINE_SOURCE }}" \
            --github-summary \
            --output-file "AIMETRegression/reports/comparison_summary.json"

      - name: Generate environment lockfile
        if: always()
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          python -m AIMETRegression.workflow.utils generate-lockfile \
            --run-id "${{ github.run_id }}"

      - name: Generate timestamp for artifacts
        if: always()
        id: timestamp
        run: |
          echo "datetime=$(date '+%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Generate baseline artifact name
        id: artifact_name
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          ARTIFACT_NAME=$(python -c "
          from AIMETRegression.workflow.artifacts import ArtifactManager, WorkflowConfig
          config = WorkflowConfig.from_env()
          manager = ArtifactManager(config)
          print(manager.get_artifact_name('${{ github.ref_name }}', 'weekly-torch'))
          ")
          echo "name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT
          echo "Generated artifact name: $ARTIFACT_NAME"

      - name: Upload baseline artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ steps.artifact_name.outputs.name }}
          path: AIMETRegression/baselines/latest.json
          retention-days: 30
          if-no-files-found: error

      - name: Upload comparison summary
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: torch-weekly-comparison-summary
          path: AIMETRegression/reports/comparison_summary.json
          retention-days: 10
          if-no-files-found: warn

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: torch-weekly-reports-${{ steps.timestamp.outputs.datetime }}-${{ github.run_id }}
          path: AIMETRegression/reports/**/*
          retention-days: 30

  cleanup-runner-torch:
    name: Cleanup runner (torch-gpu weekly)
    if: always() && github.event.inputs.framework != 'onnx'
    needs: [ start-runner-torch, torch-weekly ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/stop-gpu-runner
        with:
          runner-ids: torch-gpu-weekly-${{ github.run_id }}-${{ github.run_attempt }}
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  #=============================================================================
  # Results
  #=============================================================================
  results:
    name: Results
    needs: [onnx-weekly, torch-weekly]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download ONNX comparison summary
        if: ${{ needs.onnx-weekly.result == 'success' }}
        uses: actions/download-artifact@v3
        with:
          name: onnx-weekly-comparison-summary
          path: summaries/onnx

      - name: Download Torch comparison summary
        if: ${{ needs.torch-weekly.result == 'success' }}
        uses: actions/download-artifact@v3
        with:
          name: torch-weekly-comparison-summary
          path: summaries/torch

      - name: Generate summary
        run: |
          python3 AIMETRegression/workflow/summary.py \
            --onnx-status "${{ needs.onnx-weekly.result }}" \
            --torch-status "${{ needs.torch-weekly.result }}" \
            --onnx-summary "summaries/onnx/comparison_summary.json" \
            --torch-summary "summaries/torch/comparison_summary.json" \
            --suite "weekly" \
            --trigger "${{ github.event_name }}" \
            >> $GITHUB_STEP_SUMMARY

      - name: Set exit status
        run: |
          ONNX_STATUS="${{ needs.onnx-weekly.result }}"
          TORCH_STATUS="${{ needs.torch-weekly.result }}"

          if [ "$ONNX_STATUS" = "failure" ] || [ "$TORCH_STATUS" = "failure" ]; then
            echo "One or more regression jobs failed"
            exit 1
          fi
name: Nightly Regression

on:
  schedule:
    - cron: '0 6 * * *'

  workflow_dispatch:
    inputs:
      framework:
        description: 'Framework to run'
        required: false
        type: choice
        options:
          - all
          - onnx
          - torch
        default: 'all'

      suite:
        description: 'Test suite'
        required: false
        type: choice
        options:
          - nightly
          - smoke
        default: 'nightly'

      baseline_source:
        description: 'Branch to compare baseline against'
        required: false
        type: choice
        options:
          - own
          - develop
          - previous-release
          - custom
        default: 'own'

      baseline_source_custom:
        description: 'Custom branch name (only used when baseline_source is "custom")'
        required: false
        type: string
        default: ''

jobs:
  #=============================================================================
  # Build AIMET Wheels (Parallel)
  #=============================================================================
  build-aimet-onnx-gpu:
    name: Build AIMET (ONNX + GPU)
    if: ${{ github.event.inputs.framework != 'torch' }}
    uses: ./.github/workflows/build-wheels.yml
    with:
      # TODO: runs-on can be removed once ephemeral runners are fully adopted —
      # the runner-id is dynamically generated and does not use this value.
      variants: >
        {"include":[{
          "id":"onnx-gpu",
          "runs-on":"k8s-gpu-release",
          "VER_PYTHON":"3.10",
          "VER_ONNXRUNTIME":"1.19.2",
          "VER_CUDA":"12.6.3",
          "ENABLE_TESTS":"OFF",
          "PIP_INDEX":""
        }]}
      image-tag: latest
      use-ephemeral-runner: true
    secrets: inherit

  build-aimet-torch-gpu:
    name: Build AIMET (Torch + GPU)
    if: ${{ github.event.inputs.framework != 'onnx' }}
    uses: ./.github/workflows/build-wheels.yml
    with:
      # TODO: runs-on can be removed once ephemeral runners are fully adopted —
      # the runner-id is dynamically generated and does not use this value.
      variants: >
        {"include":[{
          "id":"torch-gpu",
          "runs-on":"k8s-gpu-release",
          "VER_PYTHON":"3.10",
          "VER_TORCH":"2.*",
          "VER_CUDA":"12.6.3",
          "ENABLE_TESTS":"OFF",
          "PIP_INDEX":""
        }]}
      image-tag: latest
      use-ephemeral-runner: true
    secrets: inherit

  #=============================================================================
  # ONNX Regression
  #=============================================================================
  start-runner-onnx:
    name: Start runner (onnx-gpu regression)
    needs: build-aimet-onnx-gpu
    if: ${{ github.event.inputs.framework != 'torch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/start-gpu-runner
        with:
          runner-id: onnx-gpu-nightly-${{ github.run_id }}-${{ github.run_attempt }}
          gpu-request: "1"
          docker-image: ${{ vars.DOCKER_REGISTRY }}/${{ vars.DOCKER_IMAGE }}-onnx-gpu:latest
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  onnx-regression:
    name: ONNX Regression
    needs: start-runner-onnx
    if: ${{ github.event.inputs.framework != 'torch' }}
    runs-on: onnx-gpu-nightly-${{ github.run_id }}-${{ github.run_attempt }}

    timeout-minutes: 720

    env:
      CUDA_VISIBLE_DEVICES: "0"
      PYTHONUNBUFFERED: "1"
      UV_LINK_MODE: "symlink"
      INPUT_SUITE: ${{ github.event.inputs.suite || 'nightly' }}-onnx
      WORKFLOW_FILE: "nightly-regression.yaml"
      BASELINE_SOURCE: ${{ github.event.inputs.baseline_source == 'custom' && github.event.inputs.baseline_source_custom || github.event.inputs.baseline_source || 'own' }}
      VER_TORCH: "2.*"
      VER_CUDA_SHORT: "cu126"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download AIMET wheel
        uses: actions/download-artifact@v3
        with:
          name: onnx-gpu-wheel
          path: downloads

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 \
            libxrender-dev libgomp1 git openssh-client unzip curl
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*

      - name: Setup Python environment
        run: |
          set -e
          sudo apt-get update -qq
          sudo apt-get install -y python3.10 python3.10-venv python3.10-dev

          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"

          uv venv .venv --python python3.10
          . .venv/bin/activate

          # Pin torch before qai-hub-models; otherwise qai-hub-models pulls a torch version incompatible with the pod's CUDA driver.
          # --extra-index-url is required on every subsequent install so uv cannot re-resolve torch onto a CUDA-13 wheel from plain PyPI.
          uv pip install torch==${{ env.VER_TORCH }} torchvision --index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install qai-hub qai-hub-models --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install onnxruntime-gpu==1.19.2 --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install pynvml pytest requests --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install mmpose --no-deps --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}  # Required by hrnet_pose (not in qai-hub-models extras)

      - name: Install AIMET
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          uv pip install downloads/*.whl --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          python -c "import aimet_onnx; print('AIMET version:', aimet_onnx.__version__)"

      - name: Pin numpy (avoid ml_dtypes segfault with numpy 2.x)
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          uv pip install "numpy<2"

      - name: Verify ONNX Runtime GPU
        run: |
          . .venv/bin/activate
          python -c "
          import onnxruntime as ort
          providers = ort.get_available_providers()
          print('ONNX Runtime providers:', providers)
          assert 'CUDAExecutionProvider' in providers
          "

      - name: Download baseline
        id: download_baseline
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.artifacts download-baseline \
            --output-dir AIMETRegression/baselines/downloaded

      - name: Setup baseline
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.utils setup-baseline --suite "$INPUT_SUITE"

      - name: Run test suite
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.suite_runner --suite "$INPUT_SUITE"

      - name: Compare with baseline
        run: |
          . .venv/bin/activate

          RESULTS_CSV="AIMETRegression/reports/results_${INPUT_SUITE}.csv"

          if [ ! -f "$RESULTS_CSV" ]; then
            echo "Results CSV not found: $RESULTS_CSV"
            exit 1
          fi

          python AIMETRegression/baseline_comparison.py run \
            --results "$RESULTS_CSV" \
            --suite-name "$INPUT_SUITE" \
            --baselines-dir "AIMETRegression/baselines" \
            --baseline-source "${{ steps.download_baseline.outputs.source_branch || env.BASELINE_SOURCE }}" \
            --github-summary \
            --output-file "AIMETRegression/reports/comparison_summary.json"

      - name: Generate lockfile
        if: always()
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          python -m AIMETRegression.workflow.utils generate-lockfile \
            --run-id "${{ github.run_id }}"

      - name: Generate timestamp
        if: always()
        id: timestamp
        run: |
          echo "datetime=$(date '+%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Generate artifact name
        id: artifact_name
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          ARTIFACT_NAME=$(python -c "
          from AIMETRegression.workflow.artifacts import ArtifactManager, WorkflowConfig
          config = WorkflowConfig.from_env()
          manager = ArtifactManager(config)
          print(manager.get_artifact_name('${{ github.ref_name }}', '$INPUT_SUITE'))
          ")
          echo "name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT

      - name: Upload baseline artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ steps.artifact_name.outputs.name }}
          path: AIMETRegression/baselines/latest.json
          retention-days: 30
          if-no-files-found: error

      - name: Upload comparison summary
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: onnx-comparison-summary
          path: AIMETRegression/reports/comparison_summary.json
          retention-days: 10
          if-no-files-found: warn

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: onnx-reports-${{ steps.timestamp.outputs.datetime }}-${{ github.run_id }}
          path: AIMETRegression/reports/**/*
          retention-days: 30

  cleanup-runner-onnx:
    name: Cleanup runner (onnx-gpu regression)
    if: always() && github.event.inputs.framework != 'torch'
    needs: [ start-runner-onnx, onnx-regression ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/stop-gpu-runner
        with:
          runner-ids: onnx-gpu-nightly-${{ github.run_id }}-${{ github.run_attempt }}
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  #=============================================================================
  # Torch Regression
  #=============================================================================
  start-runner-torch:
    name: Start runner (torch-gpu regression)
    needs: build-aimet-torch-gpu
    if: ${{ github.event.inputs.framework != 'onnx' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/start-gpu-runner
        with:
          runner-id: torch-gpu-nightly-${{ github.run_id }}-${{ github.run_attempt }}
          gpu-request: "1"
          docker-image: ${{ vars.DOCKER_REGISTRY }}/${{ vars.DOCKER_IMAGE }}-torch-gpu:latest
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  torch-regression:
    name: Torch Regression
    needs: start-runner-torch
    if: ${{ github.event.inputs.framework != 'onnx' }}
    runs-on: torch-gpu-nightly-${{ github.run_id }}-${{ github.run_attempt }}

    timeout-minutes: 720

    env:
      PYTHONUNBUFFERED: "1"
      PYTHONFAULTHANDLER: "1"
      INPUT_SUITE: ${{ github.event.inputs.suite || 'nightly' }}-torch
      WORKFLOW_FILE: "nightly-regression.yaml"
      BASELINE_SOURCE: ${{ github.event.inputs.baseline_source == 'custom' && github.event.inputs.baseline_source_custom || github.event.inputs.baseline_source || 'own' }}
      PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"
      UV_LINK_MODE: "symlink"
      VER_PYTHON: "3.10"
      VER_TORCH: "2.*"
      VER_CUDA_SHORT: "cu126"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download AIMET wheel
        uses: actions/download-artifact@v3
        with:
          name: torch-gpu-wheel
          path: downloads

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 \
            libxrender-dev libgomp1 git openssh-client unzip curl
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*

      - name: Setup Python environment
        run: |
          set -e
          sudo apt-get update -qq
          sudo apt-get install -y python${{ env.VER_PYTHON }} python${{ env.VER_PYTHON }}-venv python${{ env.VER_PYTHON }}-dev

          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"

          uv venv .venv --python python${{ env.VER_PYTHON }}
          . .venv/bin/activate

          # Pin torch before qai-hub-models; otherwise qai-hub-models pulls a torch version incompatible with the pod's CUDA driver.
          # --extra-index-url is required on every subsequent install so uv cannot re-resolve torch onto a CUDA-13 wheel from plain PyPI.
          uv pip install torch==${{ env.VER_TORCH }} torchvision --index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install qai-hub qai-hub-models --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install pynvml pytest requests object-detection-metrics --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          uv pip install mmpose --no-deps --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}  # Required by hrnet_pose (not in qai-hub-models extras)

      - name: Install AIMET
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          uv pip install downloads/*.whl --extra-index-url https://download.pytorch.org/whl/${{ env.VER_CUDA_SHORT }}
          python -c "import aimet_torch; print('AIMET Torch version:', aimet_torch.__version__)"

      - name: Pin numpy (avoid ml_dtypes segfault with numpy 2.x)
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          uv pip install "numpy<2"

      - name: Verify PyTorch GPU
        run: |
          . .venv/bin/activate
          python -c "
          import torch
          print('PyTorch version:', torch.__version__)
          print('CUDA available:', torch.cuda.is_available())
          if torch.cuda.is_available():
              print('CUDA device:', torch.cuda.get_device_name(0))
          assert torch.cuda.is_available(), 'CUDA not available!'
          "

      - name: Download baseline
        id: download_baseline
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.artifacts download-baseline \
            --output-dir AIMETRegression/baselines/downloaded

      - name: Setup baseline
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.workflow.utils setup-baseline --suite "$INPUT_SUITE"

      - name: Run test suite
        run: |
          . .venv/bin/activate
          python -m AIMETRegression.suite_runner --suite "$INPUT_SUITE"

      - name: Compare with baseline
        run: |
          . .venv/bin/activate

          RESULTS_CSV="AIMETRegression/reports/results_${INPUT_SUITE}.csv"

          if [ ! -f "$RESULTS_CSV" ]; then
            echo "Results CSV not found: $RESULTS_CSV"
            exit 1
          fi

          python AIMETRegression/baseline_comparison.py run \
            --results "$RESULTS_CSV" \
            --suite-name "$INPUT_SUITE" \
            --baselines-dir "AIMETRegression/baselines" \
            --baseline-source "${{ steps.download_baseline.outputs.source_branch || env.BASELINE_SOURCE }}" \
            --github-summary \
            --output-file "AIMETRegression/reports/comparison_summary.json"

      - name: Generate lockfile
        if: always()
        run: |
          . .venv/bin/activate
          export PATH="$HOME/.local/bin:$PATH"
          python -m AIMETRegression.workflow.utils generate-lockfile \
            --run-id "${{ github.run_id }}"

      - name: Generate timestamp
        if: always()
        id: timestamp
        run: |
          echo "datetime=$(date '+%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Generate artifact name
        id: artifact_name
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          . .venv/bin/activate
          ARTIFACT_NAME=$(python -c "
          from AIMETRegression.workflow.artifacts import ArtifactManager, WorkflowConfig
          config = WorkflowConfig.from_env()
          manager = ArtifactManager(config)
          print(manager.get_artifact_name('${{ github.ref_name }}', '$INPUT_SUITE'))
          ")
          echo "name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT

      - name: Upload baseline artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ steps.artifact_name.outputs.name }}
          path: AIMETRegression/baselines/latest.json
          retention-days: 30
          if-no-files-found: error

      - name: Upload comparison summary
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: torch-comparison-summary
          path: AIMETRegression/reports/comparison_summary.json
          retention-days: 10
          if-no-files-found: warn

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: torch-reports-${{ steps.timestamp.outputs.datetime }}-${{ github.run_id }}
          path: AIMETRegression/reports/**/*
          retention-days: 30

  cleanup-runner-torch:
    name: Cleanup runner (torch-gpu regression)
    if: always() && github.event.inputs.framework != 'onnx'
    needs: [ start-runner-torch, torch-regression ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/stop-gpu-runner
        with:
          runner-ids: torch-gpu-nightly-${{ github.run_id }}-${{ github.run_attempt }}
          kubeconfig: ${{ secrets.KUBECONFIG }}
          pat-token: ${{ secrets.SOURCE_REPO_PAT }}

  #=============================================================================
  # Results
  #=============================================================================
  results:
    name: Results
    needs: [onnx-regression, torch-regression]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download ONNX comparison summary
        if: ${{ needs.onnx-regression.result == 'success' }}
        uses: actions/download-artifact@v3
        with:
          name: onnx-comparison-summary
          path: summaries/onnx

      - name: Download Torch comparison summary
        if: ${{ needs.torch-regression.result == 'success' }}
        uses: actions/download-artifact@v3
        with:
          name: torch-comparison-summary
          path: summaries/torch

      - name: Generate summary
        run: |
          python3 AIMETRegression/workflow/summary.py \
            --onnx-status "${{ needs.onnx-regression.result }}" \
            --torch-status "${{ needs.torch-regression.result }}" \
            --onnx-summary "summaries/onnx/comparison_summary.json" \
            --torch-summary "summaries/torch/comparison_summary.json" \
            --suite "${{ github.event.inputs.suite || 'nightly' }}" \
            --trigger "${{ github.event_name }}" \
            >> $GITHUB_STEP_SUMMARY

      - name: Set exit status
        run: |
          ONNX_STATUS="${{ needs.onnx-regression.result }}"
          TORCH_STATUS="${{ needs.torch-regression.result }}"

          # Fail if any non-skipped job failed
          if [ "$ONNX_STATUS" = "failure" ] || [ "$TORCH_STATUS" = "failure" ]; then
            echo "One or more regression jobs failed"
            exit 1
          fi